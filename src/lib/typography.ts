/** Root html font-size in px at 100% scale — keep in sync with `src/styles.css`. */
export const ROOT_FONT_SIZE_PX = 16;

/** Smallest rem-based label size (uppercase meta, table badges). Prefer `text-xs` for captions. */
export const TEXT_MICRO_CLASS = "text-micro";
