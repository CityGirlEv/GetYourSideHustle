# Project Memory

## Core
Every new exported function in `src/lib/*.ts` must ship with a Vitest unit test in `src/lib/__tests__/<name>.test.ts`. Add or update the test in the same change as the function.
